import * as React from 'react';

interface Props {
    /**
     * sdsadadsa
     */
    onClick: () => voide;
}

export const Button = (props: Props) => <button type="button" {...props}>Hello</button>